"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const appium_adb_1 = require("appium-adb");
const desired_1 = require("./desired");
const session_1 = require("./helpers/session");
const bluebird_1 = __importDefault(require("bluebird"));
const asyncbox_1 = require("asyncbox");
const chai_1 = __importStar(require("chai"));
const chai_as_promised_1 = __importDefault(require("chai-as-promised"));
chai_1.default.use(chai_as_promised_1.default);
const APIDEMOS_SPLIT_TOUCH_ACTIVITY = '.view.SplitTouchView';
const DEFAULT_ADB_PORT = 5037;
async function killAndPrepareServer(oldPort, newPort) {
    const oldAdb = await appium_adb_1.ADB.createADB({ adbPort: oldPort });
    await oldAdb.killServer();
    if (process.env.CI) {
        // on Travis this takes a while to get into a good state
        await bluebird_1.default.delay(10000);
    }
    const newAdb = await appium_adb_1.ADB.createADB({ adbPort: newPort });
    await (0, asyncbox_1.retryInterval)(5, 500, async () => await newAdb.getApiLevel());
}
describe('createSession', function () {
    let driver;
    describe('default adb port', function () {
        afterEach(async function () {
            await (0, session_1.deleteSession)();
        });
        it('should start android session focusing on default pkg and act', async function () {
            driver = await (0, session_1.initSession)(desired_1.APIDEMOS_CAPS);
            await (0, chai_1.expect)(driver.getCurrentPackage()).to.eventually.equal(desired_1.APIDEMOS_PACKAGE);
            await (0, chai_1.expect)(driver.getCurrentActivity()).to.eventually.equal(desired_1.APIDEMOS_MAIN_ACTIVITY);
        });
        it('should start android session focusing on custom pkg and act', async function () {
            const caps = (0, desired_1.amendCapabilities)(desired_1.APIDEMOS_CAPS, {
                'appium:appPackage': desired_1.APIDEMOS_PACKAGE,
                'appium:appActivity': APIDEMOS_SPLIT_TOUCH_ACTIVITY,
            });
            driver = await (0, session_1.initSession)(caps);
            await (0, chai_1.expect)(driver.getCurrentPackage()).to.eventually.equal(desired_1.APIDEMOS_PACKAGE);
            await (0, chai_1.expect)(driver.getCurrentActivity()).to.eventually.equal(APIDEMOS_SPLIT_TOUCH_ACTIVITY);
        });
        it('should error out for not apk extension', async function () {
            const caps = (0, desired_1.amendCapabilities)(desired_1.APIDEMOS_CAPS, {
                'appium:app': 'foo',
                'appium:appPackage': desired_1.APIDEMOS_PACKAGE,
                'appium:appActivity': APIDEMOS_SPLIT_TOUCH_ACTIVITY,
            });
            await (0, chai_1.expect)((0, session_1.initSession)(caps)).to.eventually.be.rejectedWith(/does not exist or is not accessible/);
        });
        it('should error out for invalid app path', async function () {
            const caps = (0, desired_1.amendCapabilities)(desired_1.APIDEMOS_CAPS, {
                'appium:app': 'foo.apk',
                'appium:appPackage': desired_1.APIDEMOS_PACKAGE,
                'appium:appActivity': APIDEMOS_SPLIT_TOUCH_ACTIVITY,
            });
            await (0, chai_1.expect)((0, session_1.initSession)(caps)).to.eventually.be.rejectedWith(/does not exist or is not accessible/);
        });
    });
    describe('custom adb port', function () {
        const adbPort = 5042;
        let driver;
        beforeEach(async function () {
            await killAndPrepareServer(DEFAULT_ADB_PORT, adbPort);
        });
        afterEach(async function () {
            await (0, session_1.deleteSession)();
            await killAndPrepareServer(adbPort, DEFAULT_ADB_PORT);
        });
        it('should start android session with a custom adb port', async function () {
            const caps = (0, desired_1.amendCapabilities)(desired_1.APIDEMOS_CAPS, {
                'appium:adbPort': adbPort,
                'appium:allowOfflineDevices': true,
            });
            driver = await (0, session_1.initSession)(caps, { adbPort });
            await (0, chai_1.expect)(driver.getCurrentPackage()).to.eventually.equal(desired_1.APIDEMOS_PACKAGE);
            await (0, chai_1.expect)(driver.getCurrentActivity()).to.eventually.equal(desired_1.APIDEMOS_MAIN_ACTIVITY);
        });
    });
});
//# sourceMappingURL=driver-e2e-specs.js.map