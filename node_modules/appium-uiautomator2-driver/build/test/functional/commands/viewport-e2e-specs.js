"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const sharp_1 = __importDefault(require("sharp"));
const desired_1 = require("../desired");
const session_1 = require("../helpers/session");
const chai_1 = __importStar(require("chai"));
const chai_as_promised_1 = __importDefault(require("chai-as-promised"));
chai_1.default.use(chai_as_promised_1.default);
describe('testViewportCommands', function () {
    let driver;
    const caps = desired_1.SCROLL_CAPS;
    before(async function () {
        driver = await (0, session_1.initSession)(caps);
    });
    after(async function () {
        if (driver) {
            await (0, session_1.deleteSession)();
        }
    });
    beforeEach(function () {
        (0, session_1.attemptToDismissAlert)(caps);
    });
    it('should get device pixel ratio, status bar height, and viewport rect', async function () {
        if (process.env.CI) {
            return this.skip();
        }
        const { viewportRect, statBarHeight, pixelRatio } = await driver.getSession();
        (0, chai_1.expect)(pixelRatio).not.to.be.empty;
        (0, chai_1.expect)(statBarHeight).to.be.greaterThan(0);
        (0, chai_1.expect)(viewportRect).to.have.keys(['left', 'top', 'width', 'height']);
    });
    it('should get scrollable element', async function () {
        await (0, chai_1.expect)(driver.$('//*[@scrollable="true"]').elementId).to.eventually.exist;
    });
    it('should get content size from scrollable element found as uiobject', async function () {
        if (process.env.CI) {
            return this.skip();
        }
        const scrollableEl = await driver.$('//*[@scrollable="true"]');
        const contentSize = await scrollableEl.getAttribute('contentSize');
        (0, chai_1.expect)(contentSize).to.exist;
        (0, chai_1.expect)(JSON.parse(contentSize).scrollableOffset).to.exist;
    });
    it('should get content size from scrollable element found as uiobject2', async function () {
        if (process.env.CI) {
            return this.skip();
        }
        const scrollableEl = await driver.$('//android.widget.ScrollView');
        const contentSize = await scrollableEl.getAttribute('contentSize');
        (0, chai_1.expect)(contentSize).to.exist;
        (0, chai_1.expect)(JSON.parse(contentSize).scrollableOffset).to.exist;
    });
    it('should get first element from scrollable element', async function () {
        if (process.env.CI) {
            return this.skip();
        }
        const scrollableEl = await driver.$('//*[@scrollable="true"]');
        await (0, chai_1.expect)(scrollableEl.$('/*[@firstVisible="true"]').elementId).to.eventually.exist;
    });
    it('should get a cropped screenshot of the viewport without statusbar', async function () {
        // TODO: fails on CI with a `Does the current view have 'secure' flag set?` error
        if (process.env.CI) {
            return this.skip();
        }
        const { viewportRect, statBarHeight } = await driver.getSession();
        const fullScreen = await driver.takeScreenshot();
        const viewScreen = await driver.execute('mobile: viewportScreenshot');
        const fullB64 = Buffer.from(fullScreen, 'base64');
        const viewB64 = Buffer.from(viewScreen, 'base64');
        const fullImgMeta = await (0, sharp_1.default)(fullB64).metadata();
        const viewImgMeta = await (0, sharp_1.default)(viewB64).metadata();
        (0, chai_1.expect)(viewportRect.top).to.eql(statBarHeight);
        (0, chai_1.expect)(viewImgMeta.height).to.eql(viewportRect.height);
        (0, chai_1.expect)(viewImgMeta.width).to.eql(fullImgMeta.width);
        (0, chai_1.expect)(fullImgMeta.height).to.be.above(viewImgMeta.height);
    });
});
//# sourceMappingURL=viewport-e2e-specs.js.map