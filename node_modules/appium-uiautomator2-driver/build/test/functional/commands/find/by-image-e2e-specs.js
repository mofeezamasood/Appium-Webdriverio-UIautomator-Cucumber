"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const bluebird_1 = __importDefault(require("bluebird"));
const node_path_1 = __importDefault(require("node:path"));
const desired_1 = require("../../desired");
const session_1 = require("../../helpers/session");
const chai_1 = __importStar(require("chai"));
const chai_as_promised_1 = __importDefault(require("chai-as-promised"));
chai_1.default.use(chai_as_promised_1.default);
const START_IMG = node_path_1.default.resolve(__dirname, '..', '..', 'assets', 'start-button.png');
const STOP_IMG = node_path_1.default.resolve(__dirname, '..', '..', 'assets', 'stop-button.png');
const SQUARES_IMG = node_path_1.default.resolve(__dirname, '..', '..', 'assets', 'checkered-squares.png');
describe('Find - Image', function () {
    let driver;
    before(async function () {
        // TODO: @appium/images-plugin needs to be installed
        this.skip();
        const caps = (0, desired_1.amendCapabilities)(desired_1.APIDEMOS_CAPS, {
            'appium:appActivity': '.view.ChronometerDemo',
        });
        driver = await (0, session_1.initSession)(caps);
        // use the driver settings that cause the most code paths to be exercised
        await driver.updateSettings({
            fixImageTemplateSize: true,
            autoUpdateImageElementPosition: true,
        });
    });
    after(async function () {
        await (0, session_1.deleteSession)();
    });
    it('should find image elements', async function () {
        const els = await driver.$$(START_IMG);
        (0, chai_1.expect)(els).to.have.length(1);
    });
    it('should find an image element', async function () {
        const el = await driver.$(START_IMG);
        const value = await el.getValue();
        (0, chai_1.expect)(value).to.match(/appium-image-element/);
    });
    it('should not find an image element that is not matched', async function () {
        await (0, chai_1.expect)(driver.$(SQUARES_IMG)).to.eventually.be.rejectedWith(/Error response status: 7/);
    });
    it('should find anything with a threshold low enough', async function () {
        const { imageMatchThreshold } = await driver.getSettings();
        await driver.updateSettings({ imageMatchThreshold: 0 });
        try {
            await (0, chai_1.expect)(driver.$(SQUARES_IMG).elementId).to.eventually.exist;
        }
        finally {
            await driver.updateSettings({ imageMatchThreshold });
        }
    });
    it('should be able to get basic element properties', async function () {
        const el = await driver.$(START_IMG);
        await (0, chai_1.expect)(el.isDisplayed()).to.eventually.be.true;
        const size = await el.getSize();
        (0, chai_1.expect)(size.width).to.be.above(0);
        (0, chai_1.expect)(size.height).to.be.above(0);
        const loc = await el.getLocation();
        (0, chai_1.expect)(loc.x).to.be.at.least(0);
        (0, chai_1.expect)(loc.y).to.be.at.least(0);
        // TODO: getLocationInView requires an argument - skipping for now
        // const locInView = await el.getLocationInView();
        // expect(locInView.x).to.eql(loc.x);
        // expect(locInView.y).to.eql(loc.y);
    });
    it('should be able to click an element', async function () {
        // start and stop the chronometer using images, and then verify the time
        await driver.$(START_IMG).click();
        await bluebird_1.default.delay(3000);
        await driver.$(STOP_IMG).click();
        const readout = await driver.$("//*[contains(@text, 'Initial format')]");
        const text = await readout.getText();
        const match = /Initial format: \d\d:(\d\d)/.exec(text);
        const secs = parseInt(match[1], 10);
        (0, chai_1.expect)(secs).to.be.above(2);
        (0, chai_1.expect)(secs).to.be.below(20);
    });
});
//# sourceMappingURL=by-image-e2e-specs.js.map