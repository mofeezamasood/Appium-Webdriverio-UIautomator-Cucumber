"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const session_1 = require("../../helpers/session");
const desired_1 = require("../../desired");
const chai_1 = __importStar(require("chai"));
const chai_as_promised_1 = __importDefault(require("chai-as-promised"));
chai_1.default.use(chai_as_promised_1.default);
describe('Find - android ui elements', function () {
    let driver;
    before(async function () {
        driver = await (0, session_1.initSession)(desired_1.SETTINGS_CAPS);
    });
    after(async function () {
        if (driver) {
            await (0, session_1.deleteSession)();
        }
    });
    it('should not find statusBarBackground element via xpath', async function () {
        const statusBar = await driver.$$(`//*[@resource-id='android:id/statusBarBackground']`); //check server (NPE) if allowInvisibleElements is unset on server side
        (0, chai_1.expect)(statusBar.length).to.be.equal(0);
        await driver.updateSettings({ allowInvisibleElements: false });
        const statusBarWithInvisibleEl = await driver.$$(`//*[@resource-id='android:id/statusBarBackground']`);
        (0, chai_1.expect)(statusBarWithInvisibleEl.length).to.be.equal(0);
    });
    it('should find statusBarBackground element via xpath', async function () {
        await driver.updateSettings({ allowInvisibleElements: true });
        await (0, chai_1.expect)(driver.$(`//*[@resource-id='android:id/statusBarBackground']`).elementId).to.eventually.exist;
    });
});
//# sourceMappingURL=find-system-ui-el-e2e-specs.js.map